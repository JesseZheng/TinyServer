##############################
## TinyServer Configuration ##
##############################

# Server’s default document root directory
ROOTDIR=../www

# Default listen port
PORT=8080

# Default request handling method (fork|thread)
METHOD=thread
